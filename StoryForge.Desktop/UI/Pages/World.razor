@page "/world"
@using StoryForge.Core.Data
@inject BreadCrumbHandler crumbHandler;

<PageTitle>@Title</PageTitle>
<MudText Typo="Typo.h4" Class="mb-4">@Title</MudText>

<MudStack Spacing="4">
    <MudStack Row="true" Justify="Justify.FlexEnd">
        <MudButton Variant="Variant.Filled" StartIcon="@Icons.Material.Filled.Add" Color="Color.Primary">New Setting</MudButton>
    </MudStack>
    <ButtonList T="StorySettingEntry" Items="SettingList" PathPrefix="/world" OnItemClick="OnLocationSelected" />
</MudStack>

@code {
    const string Title = "World";

    List<StorySettingEntry> SettingList { get; } = [new StorySettingEntry(StorySettingId.New(), "Gaia")];

    protected override void OnInitialized()
    {
        crumbHandler.Items.Clear();
        crumbHandler.Items.Add(new(Title, "/world", Icons.Material.Filled.NaturePeople));
        crumbHandler.NavigationChange?.Invoke();
    }

    void OnLocationSelected(StorySettingEntry plot)
    {
        
    }
}
